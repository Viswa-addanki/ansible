- name: isntall and run nginx
  hosts: web
  become: yes # equal to b in adhoc commands
  tasks:
  - name: installing nginx
    ansible.builtin.dnf:
      name: ngnix
      state: latest
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
    