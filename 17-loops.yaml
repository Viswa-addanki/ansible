- name: install packages
  hosts: web
  become: yes
  tasks:
  - name: install
    ansible.builtin.dnf: 
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: 'mysql', state: 'absent'}
    - { name: 'nginx', state: 'absent'}
  
   